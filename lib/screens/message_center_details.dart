import 'package:flutter/material.dart';

class MessageCenterDetails extends StatelessWidget {
  const MessageCenterDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
        child: Scaffold(

        )
    );
  }
}
