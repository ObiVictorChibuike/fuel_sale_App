import 'package:flutter/material.dart';
class PlaceYourOrder extends StatelessWidget {
  const PlaceYourOrder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      top: false,
        bottom: false,
        child: Scaffold(
          body: Column(
            children: [
              SizedBox()
            ],
          ),
        ),
    );
  }
}
